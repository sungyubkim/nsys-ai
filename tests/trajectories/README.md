# Trajectory Test Files

Ground-truth trajectories generated by `scripts/gen_trajectories.py`.

## Directory Structure

```
tests/trajectories/
  A_absolute/      Q1–Q20    Absolute kernel occurrence & navigation
  B_iteration/     Q21–Q40   Iteration & layer semantics
  C_bottleneck/    Q41–Q60   Performance bottlenecks & extremes
  D_nccl/          Q61–Q80   NCCL / multi-GPU collective ops
  E_streams/       Q81–Q100  Stream utilization, overlap efficiency, system-level analysis
  F_analysis/      Q101–Q130 Analysis & diagnosis (explain, compare, recommend)
```

## JSON Schema

```json
{
  "id":       "q001",
  "version":  1,
  "suite":    "agent-absolute",
  "profile":  { "path": "data/..." },
  "model":    { "id": "gemini/gemini-2.5-flash" },
  "test_case": "What is the very first kernel?",
  "entrypoint": {
    "kind": "agent",
    "command": "run_agent_loop",
    "args": { "question": "..." }
  },
  "raw_answer": "The first kernel is ...",
  "raw_actions": [ { "type": "navigate_to_kernel", ... } ],
  "expected_tool_calls": [
    {
      "name": "query_profile_db",
      "min_calls": 1,
      "max_calls": 5,
      "sql_fragments": ["CUPTI_ACTIVITY_KIND_KERNEL", "ORDER BY", "LIMIT"],
      "raw_sql_calls": ["SELECT ..."]
    }
  ],
  "expected_response": {
    "must_contain": [],
    "must_not_contain": []
  }
}
```

## Generating Trajectories

```bash
# Single question (Q1 only)
python scripts/gen_trajectories.py data/nsys-hero/distca-0/baseline.t128k.host-fs-mbz-gpu-899.sqlite --start 1 --end 1

# Full category A (Q1–Q20)
python scripts/gen_trajectories.py data/nsys-hero/distca-0/baseline.t128k.host-fs-mbz-gpu-899.sqlite --start 1 --end 20

# All 100 questions
python scripts/gen_trajectories.py data/nsys-hero/distca-0/baseline.t128k.host-fs-mbz-gpu-899.sqlite
```

## After Generation

1. Review `raw_answer` in each JSON file.
2. Add key facts to `expected_response.must_contain`
   (e.g., kernel names, timings, stream numbers the answer mentions).
3. Commit the trajectory files — they become CI golden records.

## Running Tests

```bash
pytest tests/test_trajectories.py -v
# Skips if profile file is missing or API key is not set.
```
