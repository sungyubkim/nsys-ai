{
  "id": "q107",
  "version": 1,
  "suite": "agent-analysis",
  "profile": {
    "path": "data/nsys-hero/distca-0/baseline.t128k.host-fs-mbz-gpu-899.sqlite"
  },
  "model": {
    "id": "gemini/gemini-2.5-flash",
    "family": "gemini",
    "name": "gemini-2.5-flash",
    "temperature": 0.0
  },
  "test_case": "How much time does the forward pass take on average compared to the backward pass?",
  "entrypoint": {
    "kind": "agent",
    "command": "run_agent_loop",
    "args": {
      "question": "How much time does the forward pass take on average compared to the backward pass?"
    }
  },
  "raw_answer": "The forward pass takes, on average, approximately 10.87 milliseconds, while the backward pass takes about 0.29 milliseconds. This means the forward pass is significantly longer than the backward pass in this profile.",
  "raw_actions": [],
  "expected_tool_calls": [
    {
      "name": "query_profile_db",
      "min_calls": 1,
      "max_calls": 5,
      "sql_fragments": [
        "NVTX_EVENTS",
        "STRINGIDS",
        "WHERE"
      ],
      "raw_sql_calls": [
        "\nSELECT\n    CASE\n        WHEN StringIds.value LIKE '%forward%' THEN 'forward pass'\n        WHEN NVTX_EVENTS.text LIKE '%forward%' THEN 'forward pass'\n        WHEN StringIds.value LIKE '%backward%' THEN 'backward pass'\n        WHEN NVTX_EVENTS.text LIKE '%backward%' THEN 'backward pass'\n        ELSE 'other'\n    END AS pass_type,\n    AVG(NVTX_EVENTS.end - NVTX_EVENTS.start) AS avg_duration_ns\nFROM NVTX_EVENTS\nLEFT JOIN StringIds ON NVTX_EVENTS.textId = StringIds.id\nWHERE (StringIds.value LIKE '%forward%' OR NVTX_EVENTS.text LIKE '%forward%')\n   OR (StringIds.value LIKE '%backward%' OR NVTX_EVENTS.text LIKE '%backward%')\nGROUP BY pass_type;\n"
      ]
    }
  ],
  "expected_response": {
    "must_contain": [],
    "must_not_contain": []
  }
}