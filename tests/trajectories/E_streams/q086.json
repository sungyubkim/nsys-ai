{
  "id": "q086",
  "version": 1,
  "suite": "agent-streams",
  "profile": {
    "path": "data/nsys-hero/distca-0/baseline.t128k.host-fs-mbz-gpu-899.sqlite"
  },
  "model": {
    "id": "gemini/gemini-2.5-flash",
    "family": "gemini",
    "name": "gemini-2.5-flash",
    "temperature": 0.0
  },
  "test_case": "Is kernel launch overhead a bottleneck? How many kernels are shorter than 10 µs, and what is their combined overhead relative to total GPU time?",
  "entrypoint": {
    "kind": "agent",
    "command": "run_agent_loop",
    "args": {
      "question": "Is kernel launch overhead a bottleneck? How many kernels are shorter than 10 µs, and what is their combined overhead relative to total GPU time?"
    }
  },
  "raw_answer": "There are 11,051 kernels shorter than 10 µs. Their combined duration is 45.42 ms, which represents about 0.03% of the total GPU time. This indicates that kernel launch overhead is unlikely to be a significant bottleneck in this profile.",
  "raw_actions": [],
  "expected_tool_calls": [
    {
      "name": "query_profile_db",
      "min_calls": 1,
      "max_calls": 5,
      "sql_fragments": [
        "CUPTI_ACTIVITY_KIND_KERNEL",
        "WHERE"
      ],
      "raw_sql_calls": [
        "SELECT COUNT(*) AS short_kernel_count, SUM(end - start) AS short_kernel_duration_ns FROM CUPTI_ACTIVITY_KIND_KERNEL WHERE (end - start) < 10000;",
        "SELECT SUM(end - start) AS total_gpu_duration_ns FROM CUPTI_ACTIVITY_KIND_KERNEL;"
      ]
    }
  ],
  "expected_response": {
    "must_contain": [
      "0.03%"
    ],
    "must_not_contain": []
  }
}