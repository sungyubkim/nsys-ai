{
  "id": "q095",
  "version": 1,
  "suite": "agent-streams",
  "profile": {
    "path": "data/nsys-hero/distca-0/baseline.t128k.host-fs-mbz-gpu-899.sqlite"
  },
  "model": {
    "id": "gemini/gemini-2.5-flash",
    "family": "gemini",
    "name": "gemini-2.5-flash",
    "temperature": 0.0
  },
  "test_case": "Is there evidence of stream execution fragmentation (many short kernels back-to-back with gaps)? Which stream is most fragmented, and what is the overhead?",
  "entrypoint": {
    "kind": "agent",
    "command": "run_agent_loop",
    "args": {
      "question": "Is there evidence of stream execution fragmentation (many short kernels back-to-back with gaps)? Which stream is most fragmented, and what is the overhead?"
    }
  },
  "raw_answer": "Stream 21 has the most kernels (39900), followed by stream 56 (4725). These streams are potentially more fragmented.\n\nTo investigate further for stream 21, you could zoom into a section of the timeline in Nsight Systems and visually inspect the kernel execution and the gaps between them. For example, to zoom to the first 10 milliseconds of the trace to look at stream 21:",
  "raw_actions": [
    {
      "type": "zoom_to_time_range",
      "start_s": 0.0,
      "end_s": 0.01
    }
  ],
  "expected_tool_calls": [
    {
      "name": "query_profile_db",
      "min_calls": 1,
      "max_calls": 5,
      "sql_fragments": [
        "CUPTI_ACTIVITY_KIND_KERNEL",
        "ORDER BY",
        "LIMIT"
      ],
      "raw_sql_calls": [
        "SELECT streamId, COUNT(*) AS kernel_count FROM CUPTI_ACTIVITY_KIND_KERNEL GROUP BY streamId ORDER BY kernel_count DESC LIMIT 10;"
      ]
    }
  ],
  "expected_response": {
    "must_contain": [],
    "must_not_contain": []
  }
}